# ============================================================
# .gitignore — MyStorage
# ============================================================
 
# NOTE: supabase-config.js sengaja tidak di-ignore karena SUPABASE_ANON_KEY
# adalah publishable key yang aman untuk frontend (bukan service_role key).
# Pastikan RLS (Row Level Security) aktif di semua tabel Supabase.
 
# Environment files
.env
.env.local
.env.production
 
# OS & Editor
.DS_Store
Thumbs.db
.vscode/
.idea/